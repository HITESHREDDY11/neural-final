'use client';

/**
 * NavigationOverlay.tsx — disabled.
 * The full-viewport cover/fade was removed to ensure instant route transitions
 * with zero animation delay on navigation.
 */

export default function NavigationOverlay() {
  return null;
}
